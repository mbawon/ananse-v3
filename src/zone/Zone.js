import React, { Fragment } from 'react'
import allStyles from '../assets/css/all-styles.module.css'

const Zones = () =>{

    return(
        <Fragment>
            <div className={allStyles.page__header}>
                <span className={allStyles.pageTitle}>Zone List</span>
            </div>
        </Fragment>
    )
}

export default Zones